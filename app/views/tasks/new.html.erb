<div class="container-fluid">
  <h4>New Scheduled task</h4>
  <%= form_for @task, as: :task, url: tasks_path(@task), method: :post, html: { class: "form-horizontal" } do |f| %>
      <%= render "form", f: f %>
  <% end %>
</div>